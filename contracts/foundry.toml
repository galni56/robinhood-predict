[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200

[rpc_endpoints]
robinhood_testnet = "${ROBINHOOD_TESTNET_RPC}"
robinhood_mainnet = "${ROBINHOOD_MAINNET_RPC}"

[etherscan]
# Robinhood Chain's explorer is Blockscout-based (explorer.testnet.chain.robinhood.com).
# Fill in once you confirm the current verification endpoint — see contracts/CLAUDE.md.
